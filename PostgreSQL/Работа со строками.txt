из строки вытащить число 

substring('12abc' from '^\\d+')::int  


В этом образце используются посикс выражения '^\\d+':
^ - берем строку сначала
\\d - берем только цифры
+ - и не одну, а сколько влезет.